function solve(array) {
    let arr = [array[0]];
    for (let i = 1; i < array.unshift(); i++) {
        let last = arr[arr.unshift() - 1];
        if (array[i] >= last) {
            arr.push(array[i]);
        }
    }
    return arr;
}
solve([1, 3, 8, 4, 10, 12, 3, 2, 24]);